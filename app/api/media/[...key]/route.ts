import {env} from "cloudflare:workers";
export const dynamic="force-dynamic";
export async function GET(request:Request,{params}:{params:Promise<{key:string[]}>}){const {key}=await params;const object=await env.BUCKET.get(key.join("/"));if(!object)return new Response("Arquivo não encontrado",{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set("etag",object.httpEtag);headers.set("cache-control","public, max-age=86400");const download=new URL(request.url).searchParams.get("download")==="1";if(download){const original=(object.customMetadata?.originalName||key.at(-1)||"arquivo").replace(/[\r\n"]/g,"");headers.set("content-disposition",`attachment; filename="${original}"; filename*=UTF-8''${encodeURIComponent(original)}`)}return new Response(object.body,{headers})}
