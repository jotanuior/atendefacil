"use client";import {useState} from "react";
export default function Reset(){const[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm)return setError("As senhas não são iguais");const token=new URLSearchParams(location.search).get("token");const r=await fetch("/api/auth/reset",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password})});const d=await r.json();if(r.ok)setDone(true);else setError(d.error)}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="auth-brand"><b>A</b><strong>Atende<span>Fácil</span></strong></div><h1>Criar nova senha</h1>{done?<><div className="auth-success">Senha alterada com sucesso.</div><a className="auth-main-link" href="/login">Entrar no sistema</a></>:<>{error&&<div className="auth-error">{error}</div>}<label>Nova senha</label><input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/><label>Confirmar senha</label><input type="password" minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)} required/><button>Alterar senha</button></>}</form></main>}
