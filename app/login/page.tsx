"use client";import {appPath} from "../lib/paths";import {useState} from "react";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch(appPath("/api/auth/login"),{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();setBusy(false);if(r.ok)location.href=appPath("/");else setError(d.error||"Não foi possível entrar")}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="auth-brand"><b>A</b><strong>Atende<span>Fácil</span></strong></div><small>ÁREA ADMINISTRATIVA</small><h1>Entrar no sistema</h1><p>Use seu e-mail e senha de administrador.</p>{error&&<div className="auth-error">{error}</div>}<label>E-mail</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/><label>Senha</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/><button disabled={busy}>{busy?"Entrando...":"Entrar"}</button><a href={appPath("/esqueci-senha")}>Esqueci minha senha</a><hr/><a href={appPath("/atendimento")}>Ir para o atendimento público</a></form></main>}
