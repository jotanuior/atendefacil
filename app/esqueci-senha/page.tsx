"use client";import {appPath} from "../lib/paths";import {useState} from "react";
export default function Forgot(){const[email,setEmail]=useState(""),[done,setDone]=useState(false),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch(appPath("/api/auth/forgot"),{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const d=await r.json();if(r.ok)setDone(true);else setError(d.error)}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="auth-brand"><b>A</b><strong>Atende<span>Fácil</span></strong></div><h1>Recuperar senha</h1>{done?<><div className="auth-success">Se o e-mail estiver cadastrado, você receberá um link válido por 1 hora.</div><a href={appPath("/login")}>Voltar para o login</a></>:<><p>Informe seu e-mail para receber o link de redefinição.</p>{error&&<div className="auth-error">{error}</div>}<label>E-mail</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><button>Enviar link</button><a href={appPath("/login")}>Voltar para o login</a></>}</form></main>}
